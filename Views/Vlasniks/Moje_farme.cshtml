@model IEnumerable<WebApplication2.Models.Farma>

@{
    ViewData["Title"] = "MojeFarme";
}

<h2>MojeFarme</h2>

<p>
    <a asp-action="Create" asp-controller="Farmas" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.LokalniNaziv)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Lat)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Lng)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Vlasnik)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LokalniNaziv)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lat)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lng)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vlasnik.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-controller="Farmas" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-controller="Farmas" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-controller="Farmas" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
